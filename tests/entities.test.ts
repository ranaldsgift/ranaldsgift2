import { it, expect, describe } from "vitest";

it("Classes which extend BaseEntity<TInterface> should conform to the interface", () => {
	// This test should ensure that the classes which extend BaseEntity<TInterface> conform to the TInterface interface
	// in terms of named properties so that it can be correctly converted to a POJO

	// TODO - Implement this correctly
	expect(1 + 1).toBe(2);
});
